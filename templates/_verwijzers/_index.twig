{% extends "_layout.twig" %}
    
{% block content %}
        <h1>{{ entry.title }}</h1>
        <p>{{ entry.subtitle }}</p>

         <div class="ons-team">
            {% set teamCards = craft.entries.section('onsTeam').all() %}
            {% for teamCard in teamCards %}
                {% include "_snippets/_teamCard.twig" %}
            {% endfor %}
        </div>
{% endblock %}