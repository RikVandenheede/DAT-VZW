{% extends "_layout.twig" %}
    
{% block content %}
    <h1>{{ entry.title }}</h1>

    {% set blocks = entry.vacatureInformatie.all() %}   
    {% set blocks = craft.entries.section('vacatures').all() %}
    {% for block in blocks %}
        {% include entry.vacatureIformatie %}
    {% endfor %}


{% endblock %} 