{% extends "_layout.twig" %}
    
{% block content %}
        <h1>{{ entry.title }}</h1>
        <p>{{ entry.subtitle }}</p>
        <div>
            {# {% set cards = entry.card.all() %}    #}
            {% set cards = craft.entries.section('trajectVanHetKind').all() %}   
            {% for card in cards %}
                {% include "_snippets/_card.twig" %}
            {% endfor %}
        </div>
{% endblock %}
