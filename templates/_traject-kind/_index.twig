{% extends "_layout.twig" %}
    
{% block content %}
        <h1>{{ entry.title }}</h1>
        <p>{{ entry.subtitle }}</p>

         <div class="ons-team">
            {% set trajectCards = craft.entries.section('onsTeam').all() %}
            {% for trajectCard in trajectCards %}
                {% include "_snippets/_teamCard.twig" %}
            {% endfor %}
        </div>
{% endblock %}
