<div class="card">
    <span class="card__img"></span>
    <h4 class="card__title">{{ card.cardTitle }}</h4>
    <button class="card__link btn btn--secondary" href=>{{card.buttonText}}</button>
</div>