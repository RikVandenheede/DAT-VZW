<div class="card">
    <span class="card__img"></span>
    <h4 class="card__title">{{ teamCard.title }}</h4>
    <button class="card__link btn btn--secondary" href={{teamCard.linkto}}>Bekijk in detail</button>
</div>